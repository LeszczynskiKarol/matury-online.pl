{
  "name": "matury-online",
  "private": true,
  "scripts": {
    "dev": "bash scripts/dev-start.sh",
    "dev:no-sync": "concurrently -n back,front -c blue,green \"cd backend && npm run dev\" \"cd frontend && npm run dev -- --host\"",
    "sync:db": "bash scripts/sync-db.sh"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
